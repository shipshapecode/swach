diff --git a/src/yaml.js b/src/yaml.js
index a71ea23724230c30f535460b9829f36c06b89514..585b755cd29b47db3a83de7ef5c7d054484438e2 100644
--- a/src/yaml.js
+++ b/src/yaml.js
@@ -193,9 +193,10 @@ class SnapcraftYAML {
   }
 
   transformParts (packageDir) {
-    this.parts.source = path.dirname(packageDir)
-    this.parts.organize = {}
-    this.parts.organize[path.basename(packageDir)] = this.data.name
+    // FIXED: Use packageDir directly instead of its parent directory
+    // This fixes the "unable to determine source type" error in snapcraft
+    this.parts.source = packageDir
+    // REMOVED: organize mapping since we're pointing directly to the app directory
 
     return this.updateDependencies()
   }
